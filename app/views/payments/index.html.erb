<p id="notice"><%= notice %></p>

<h1>Listing Payments</h1>

<table>
  <thead>
    <tr>
      <th>Item</th>
      <th>Person</th>
      <th>Amount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @payments.each do |payment| %>
      <tr>
        <td><%= payment.item %></td>
        <td><%= payment.person %></td>
        <td><%= number_to_currency payment.amount, unit: '£' %></td>
        <td><%= link_to 'Show', item_payment_path(@item, payment) %></td>
        <td><%= link_to 'Edit', edit_item_payment_path(@item, payment) %></td>
        <td><%= link_to 'Destroy', item_payment_path(@item, payment), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Payment', new_item_payment_path %>
