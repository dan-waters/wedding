<%= form_for @guest, :html => {:class => "form-horizontal guest"} do |f| %>

    <% if @guest.errors.any? %>
        <div id="error_expl" class="panel panel-danger">
          <div class="panel-heading">
            <h3 class="panel-title"><%= pluralize(@guest.errors.count, "error") %> prohibited this guest from being
              saved:</h3>
          </div>
          <div class="panel-body">
            <ul>
              <% @guest.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        </div>
    <% end %>

    <div class="control-group">
      <%= f.label :guest_type_id, :class => 'control-label' %>
      <div class="controls">
        <%= f.collection_select :guest_type_id, GuestType.all, :id, :description, {}, :class => 'form-control' %>
      </div>
      <%= error_span(@guest[:guest_type_id]) %>
    </div>
    <div class="control-group">
      <%= f.label :name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'form-control' %>
      </div>
      <%= error_span(@guest[:name]) %>
    </div>
    <div class="control-group">
      <%= f.label :invited, :class => 'control-label' %>
      <div class="controls">
        <%= f.check_box :invited %>
      </div>
      <%= error_span(@guest[:invited]) %>
    </div>
    <div class="control-group">
      <%= f.label :accepted, :class => 'control-label' %>
      <div class="controls">
        <%= f.check_box :accepted %>
      </div>
      <%= error_span(@guest[:accepted]) %>
    </div>


    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                guests_path, :class => 'btn btn-default' %>

<% end %>
