<h1>Daniel and Amy's Wedding</h1>
<%= render partial: 'item_table', locals: {description: 'Booked Items', items: Item.booked} %>
<%= render partial: 'item_table', locals: {description: 'Items to Book', items: Item.not_booked} %>
<div class="container">
  <h2>Money</h2>
  <dl class="dl-horizontal">
    <dt class="list-group-item-heading">Paid:</dt>
    <dd class="list-group-item-text"><%= number_to_currency Payment.all.map(&:amount).sum %></dd>
    <dt class="list-group-item-heading">Outstanding:</dt>
    <dd class="list-group-item-text"><%= number_to_currency Item.all.map(&:balance).sum %></dd>
  </dl>
</div>
<div class="container">
  <h2>Honeymoon</h2>
  <div id="map-canvas" data-url="<%= honeymoon_destinations_path %>"></div>
</div>

<script type="text/javascript">
    $(document).ready(function(){
        window.showMap();
    });
</script>